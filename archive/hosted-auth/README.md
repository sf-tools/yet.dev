# Hosted authentication archive

This folder contains the disconnected hosted sign-in prototype. It is not imported, compiled, or shipped by Yet. The active CLI authenticates only with `OPENAI_API_KEY`.
